# =============================================================================
# 基础配置
# =============================================================================
baseURL = 'https://blog.billbai.me/'
title = "bill bai's blog"
theme = "PaperMod"

# =============================================================================
# 语言与本地化
# =============================================================================
languageCode = "zh-Hans"
defaultContentLanguage = "zh"
hasCJKLanguage = true

# =============================================================================
# 构建选项
# =============================================================================
enableRobotsTXT = true
enableEmoji = true
buildDrafts = false
buildFuture = false
buildExpired = false

# =============================================================================
# 输出格式
# =============================================================================
[outputs]
  home = ["HTML", "RSS", "JSON"]

# =============================================================================
# 分页
# =============================================================================
[pagination]
  pagerSize = 42

# =============================================================================
# 缓存
# =============================================================================
[caches]
  [caches.images]
    dir = ':cacheDir/images'

# =============================================================================
# 代码高亮
# =============================================================================
[markup]
  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNos = true
    style = "catppuccin-mocha"

# =============================================================================
# 导航菜单
# =============================================================================
[menu]
  [[menu.main]]
    name = "📝Posts"
    url = "/posts/"
    weight = 2

  [[menu.main]]
    name = "🏷️Tags"
    url = "/tags/"
    weight = 3

  [[menu.main]]
    name = "🔎Search"
    url = "/search/"
    weight = 4

# =============================================================================
# 站点参数
# =============================================================================
[params]
  author = "billbai"
  description = "billbai's personal blog."
  keywords = ["blog", "billbai", "technology", "life"]
  github = "https://github.com/billbai"
  DateFormat = "2006-01-02"

  # 主题
  defaultTheme = "auto"
  disableThemeToggle = false

  # Favicon 和头像
  favicon = "/favicon.ico"
  images = ["/avatar.png"]

  # 文章显示
  ShowReadingTime = true
  ShowCodeCopyButtons = true
  ShowWordCount = true

  [params.assets]
    disableHLJS = true  # 禁用 Highlight.js，使用 Hugo 内置高亮

  [params.homeInfoParams]
    Title = "bill bai's blog"
    Content = "🐸 Where I share my thoughts on random stuff."

  [[params.socialIcons]]
    name = "github"
    url = "https://github.com/billbai"
